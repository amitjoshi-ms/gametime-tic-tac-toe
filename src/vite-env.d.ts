/// <reference types="vite/client" />

/**
 * Application version injected at build time from package.json.
 * Configured in vite.config.ts using the `define` option.
 */
declare const __APP_VERSION__: string;
